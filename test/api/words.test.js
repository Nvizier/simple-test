import { describe, expect, test } from "vitest";
import { setup, $fetch } from "@nuxt/test-utils";

describe("Words API test", async () => {
  await setup({
    // test context options
  });
  test("Checks the content of the return", async () => {});
});
