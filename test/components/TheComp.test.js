import { describe, expect, test } from "vitest";
import { setup, $fetch } from "@nuxt/test-utils";
describe("TheComp.vue component Test", async () => {
  await setup({
    // test context options
  });
  test("Checks if the component renders correct", async () => {});
});
